spring:
  application:
    name: NtSystem
  servlet:
    multipart:
      enabled: true
      max-file-size: 2MB
      max-request-size: 10MB
server:
  port: 8081

BASE_URL: "http://localhost:8080/api/v1"
